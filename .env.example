# Email Configuration (for fastapi-mail)
# Required for sending verification emails

# Gmail Example:
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
MAIL_FROM=noreply@rgcirc.com
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com

# For Gmail, you need to:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App-Specific Password:
#    - Go to: https://myaccount.google.com/apppasswords
#    - Create a new app password for "Mail"
#    - Use that password (not your regular Gmail password) as MAIL_PASSWORD

# Authentication (JWT)
SECRET_KEY=your-secret-key-here-change-in-production

# MongoDB (if using database)
# MONGODB_URL=mongodb://localhost:27017/
# MONGODB_DATABASE=rgcirc
